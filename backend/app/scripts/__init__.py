"""ASTRA — Operational scripts (seeders, one-shot maintenance utilities).

Run via `python -m app.scripts.<name>` from the backend container, e.g.::

    docker exec astra-backend-1 python -m app.scripts.seed_catalog
"""

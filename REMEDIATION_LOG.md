# ASTRA Remediation Log
**Started:** 2026-04-29
**Source audit:** `AUDIT_FINDINGS.md` (commit `2b1e8f71`)
**Branch strategy:** One branch per phase: `fix/phase-1-critical`, `fix/phase-2-security`, `fix/phase-3-medium`, `fix/phase-4-cleanup`.

## Progress

| Finding | Severity | Status | Files Touched | Commit | Verification | Notes |
|---|---|---|---|---|---|---|
| §2.1–2.4 cross-cutting | — | ✅ Built | `backend/app/dependencies/{__init__,project_access}.py`, `backend/app/middleware/body_size_limit.py`, `backend/app/services/quality/{__init__,nasa_terms}.py`, `backend/app/services/security/{__init__,record_hash}.py` | `09510cd` | `python -m ast` parse OK | New modules — used by Phase 1+ findings. Body-size middleware not yet registered (Phase 2 §4.9). |
| F-002 | Critical | ✅ Fixed | `backend/app/routers/workflow.py` → `backend/app/models/workflow.py` (moved); `backend/app/models/workflows.py` → `backend/app/routers/workflows.py` (moved); `backend/app/main.py` (logger.warning replaces silent excepts — also covers F-121); `backend/app/models/__init__.py` (re-export workflow models — also covers F-138) | `d495747` | `python -m ast` parse OK on all 4 touched files; runtime verification needs `docker exec astra-backend-1 python -c "from app.routers.workflows import router; print(len(router.routes))"` | `services/signature_service.py:18`, `services/workflow_engine.py:22`, and `routers/workflows.py:37` all import `from app.models.workflow import …` — that path NOW exists. No code change needed in those three files. F-138 + F-121 collapsed into the same commit per the plan. |
| F-001 | Critical | ✅ Fixed | `backend/app/services/auth_manager.py:54`; `backend/tests/test_auth_manager_jwt.py` (new regression test — both standard and MFA-partial paths) | `0cb1b96` | `python -m ast` parse OK; `pytest backend/tests/test_auth_manager_jwt.py` requires Docker container | One-line change: `settings.SECRET_KEY` → `settings.SECRET_KEY.get_secret_value()`. |
| F-003 + F-067 | Critical+High | ✅ Fixed | `backend/app/services/encryption.py` (full rewrite — drop literal fallback, expose `derive_key`, salt configurable via `ENCRYPTION_KEY_SALT`, `decrypt_field` re-raises by default with `ALLOW_PLAINTEXT_LEGACY=true` opt-in); `backend/app/services/mfa.py` (use `derive_key` with separate salt `b"astra-mfa-v1"`, drop byte-truncate-and-pad); `backend/app/config.py` (`enforce_production_guards` checks ENCRYPTION_KEY too, refuses empty / known-weak / <32-char in prod) | `38d6382` | `python -m ast` parse OK; runtime needs `pytest backend/tests` after Phase 2 unit tests added | Behaviour change: dev environments without ENCRYPTION_KEY *or* SECRET_KEY now raise RuntimeError on first encrypt — this is intentional per the plan ("loud crash that's easy to fix"). MFA secrets use a distinct salt so the MFA Fernet key != field-encryption Fernet key derived from the same input. |
| F-004 + F-120 + F-062 | Critical+Low+Medium | ✅ Fixed | `backend/app/routers/seed_project.py` (prefix `/dev` → `/admin/seed-project`; route `/seed-project/{project_id}` → `/{project_id}`; `Depends(require_permission("projects.create"))`; SEED-MARKER sentinel idempotency replacing `count >= 20`); `backend/app/main.py` (move import + mount inside `if not is_prod:` block) | `ad5a5b9` | `python -m ast` parse OK; full path becomes `/api/v1/admin/seed-project/{project_id}` (was `/api/v1/dev/seed-project/{project_id}`) — frontend callers via `dev/seed-project/...` need the path update tracked in cross-cutting orphan list | Defence-in-depth: env gate + auth dep + sentinel idempotency. F-120 (router prefix collision with `dev_router`) handled in the same edit. F-062 (count threshold idempotency) replaced with sentinel. Frontend callers will need a follow-up to use the new path — no frontend caller for this endpoint currently exists per the audit's orphan list. |
| F-005 | Critical | ✅ Fixed | `frontend/src/app/projects/[id]/audit/page.tsx:49` | `3a50373` | Manual: load /projects/X/audit and confirm filters + pagination return scoped data | One-character fix: `{ params }` → `{ params: p }`. |
| F-006 | Critical | ✅ Fully resolved (rotation + history rewrite) | (1) Stage 1 in `8f77963`: file moved to `../ASTRA-backups/`, `.gitignore` patterns added. (2) Stage 2 (this session): `.env` rotated; DB ALTER USER + TRUNCATE; stack restarted; mason login verified; `git filter-repo --path 4_24_2026_SQL_ASTRA.sql --invert-paths --force` then `reflog expire --all --expire=now` + `gc --prune=now --aggressive`; force-pushed both branches to origin. | `8f77963` (untrack); rewritten history: `main` `2b1e8f7→3d7732d`, `fix/phase-1-critical` `2ff174e→bbc8726`. All commits got new SHAs as filter-repo rewrites every commit. | `git log --all --oneline -- 4_24_2026_SQL_ASTRA.sql` returns 0 lines locally and on origin after force-push. `pg_restore -l ../ASTRA-backups/4_24_2026_SQL_ASTRA.sql` had previously confirmed 44 tables w/ data including `users`, `integration_configs`, `mfa_configs`, `electronic_signatures`, `refresh_tokens`. | Rotation details in the new "Secret rotation log" section below. Anyone with the prior commit SHAs in their local clone or in CI caches must re-clone (their old SHAs no longer exist on origin). |
| F-014 (part 1) | Critical | ✅ Fixed | 7 routers — `dashboard.py`, `impact.py`, `baselines.py`, `projects.py`, `requirements.py`, `integrations.py`, `reports.py` — ~46 endpoints | `449d6ad` | `python -m ast` parse OK on all 7; runtime needs an integration test that creates two projects with disjoint members and asserts every endpoint returns 403 for the non-member (per plan §3.7 verification — test scaffold deferred) | Endpoints with body-only project_id (create_baseline, create_trace_link, integration create, batch quality check) call `_check_membership` inline because FastAPI deps cannot read the body synchronously. `list_projects` is now scoped (admin sees all, others see owned + member-of). `compare_baselines` rejects cross-project comparisons. F-058 (Medium) collapsed in. F-032 partially covered (history scoping done; durability deferred to Phase 2 §4.21). |
| F-014 (part 2 — interface.py project_id-query endpoints) | Critical | ✅ Fixed | `backend/app/routers/interface.py` — `_require_project()` helper now requires `current_user` and calls `_check_membership`; covers 14 endpoints: list_systems, create_system, list_units, create_unit, search_pins, list_harnesses, search_wires, trace_signal, get_n2_matrix, get_block_diagram, get_interface_coverage, auto_grow, list_connections + payload-based variants. New `_assert_member_for_entity` helper added for use by F-014 part 3. | `d5e3bd2` | `python -m ast` parse OK | All endpoints in interface.py that take `project_id` in path/query/body now membership-checked. |
| F-014 (part 3 — interface.py entity-keyed mutation endpoints) | Critical | ✅ Fixed | `backend/app/routers/interface.py` — `_assert_member_for_entity` extended to walk parent chains for Pin / MessageField / Wire / HarnessEndpoint / PinBusAssignment, then applied inline to every entity-keyed handler. Clusters: A=Systems+Units (`e0eeaa9`), B=Connectors+Pins (`a8cbbd1`), C=Buses+PinBusAssignment (`ff7e07c`), D=Messages+Fields (`260058e`), E=Harnesses+Wires (`ac5d2e9`), F=auto-req+req-links (`cd2b794`, also closes F-046 + F-052), G=Connections+HarnessEndpoints (`a26c5c1`). | `e0eeaa9`, `a8cbbd1`, `ff7e07c`, `260058e`, `ac5d2e9`, `cd2b794`, `a26c5c1` | `python -m ast` parse OK on each cluster commit; runtime test in `backend/tests/test_project_membership.py` (commit `45a2850`) — needs Docker container (or Py 3.12 venv) to actually execute, see notes below | All ~40 entity-keyed handlers now reject non-members with 403. Bulk endpoints (approve_auto_requirements, reject_auto_requirements, bulk_delete) report mixed-project payloads in a new `forbidden` count rather than 403'ing the whole batch. |
| F-014 verification test | — | ✅ Written, ⏸ Pending Docker run | `backend/tests/test_project_membership.py` (new) | `45a2850` | `python -m ast` parse OK. Local Python 3.14 cannot install pinned `pydantic-core==2.27` / `psycopg2-binary` wheels — must run inside `astra-backend-1` Docker container or a Python 3.12 venv. Test command: `docker exec astra-backend-1 pytest backend/tests/test_project_membership.py -v` | 41-row parametrized cross-project negative test covering every router in F-014. Plus admin-bypass positive, own-project positive, cross-project trace-link rejection, and bulk-delete mixed-project semantics. |
| F-046 | Medium | ✅ Fixed | (folded into F-014 part 3 cluster F commit `cd2b794`) | `cd2b794` | n/a | per-requirement project membership in approve / reject auto-requirements; mixed-project payloads return new `forbidden` counter. |
| F-052 | Medium | ✅ Fixed | (folded into F-014 part 3 cluster F commit `cd2b794`) | `cd2b794` | n/a | `list_req_links` now requires entity_type+entity_id as a pair OR requirement_id alone; results filtered by member-project. |
| **Phase 2A — Auth & access surface** | — | ✅ Complete (22/22 tests green) | — | — | `pytest -k "auth or register or webhook or lockout or login"` → 22 passed | Branch `fix/phase-2-security`. Five findings closed across five commits. |
| F-015 | High | ✅ Fixed | `backend/app/routers/auth.py` (force `UserRole.DEVELOPER`); `backend/tests/test_auth.py::test_register_ignores_role_field` | `43dee4f` | 3/3 register tests pass | `/auth/register` ignores body `role` field; admin role must come from `/admin/users`. |
| F-016 + F-031 + F-124 | Medium+Medium+Low | ✅ Fixed (cluster) | `backend/app/routers/auth.py` (lockout wired into login flow, failed-login audit, removed silent try/except around success audit); `.env.example` (MAX_LOGIN_ATTEMPTS, LOCKOUT_DURATION_MINUTES); `backend/tests/test_auth.py` (TestLoginLockout, TestLoginAudit); `backend/tests/conftest.py` (RATE_LIMIT_* bumped to 100k so the singleton rate-limit middleware doesn't cross-contaminate sibling tests) | `4e41968` | 9/9 lockout+audit tests pass | NIST AC-7 (lockout returns **429** with Retry-After) + NIST AU-2 (login_failed audit row per failure against known user; `logger.warning` for unknown-username probes since `audit_log.user_id` is FK NOT NULL). Removed silent `try/except: pass` around success-side audit. F-010 (.env.example sweep) only partially addressed — see backlog. |
| F-017 | High | ✅ Fixed | `backend/app/routers/integrations.py` (path-slug routing `/integrations/{config_id}/jira|azure/webhook`, HMAC-SHA256 / Basic-auth verification, generic 401 on every failure); `backend/tests/test_webhooks.py` (10 tests) | `a80fe8b` | 10/10 webhook tests pass | Webhook secrets stored in `config_encrypted.webhook_secret` (no schema change). Auth runs **before** any DB write — verified by SyncLog row-count snapshots. |
| F-021 + F-104 | High+Medium | ✅ Fixed | `docker-compose.yml` (db + pgadmin ports bound to `127.0.0.1`); F-021 was already done in Task 1 rotation, verified no placeholders remain in `.env` | `cb9360e` | `docker port astra-db-1 5432` returns `127.0.0.1:5432`; backend health + login round-trip green | `nc -z 0.0.0.0 5432` from another host now fails per the audit acceptance criterion. |
| F-066 | Medium | ✅ Fixed | `backend/app/main.py` (TrustedHostMiddleware); `backend/app/config.py` (`enforce_production_guards` rejects `ALLOWED_HOSTS="*"` or empty in prod); `database/pg_hba.conf` (NEW — 127.0.0.1 + ::1 require scram-sha-256, not trust); `docker-compose.yml` (bind-mount + `-c hba_file=…`); `backend/tests/test_production_guards.py` (6 tests) | `4b73530` | 6/6 prod-guard tests pass; `psql -h 127.0.0.1 -U astra` (no/wrong password) → fails; backend login still succeeds via docker bridge | Closes the "127.0.0.1 trust" backlog item logged during Task A diagnosis. F-010 (.env.example sweep) is the only related Phase-2 item still open. |
| **Phase 2B — Compliance integrity** | — | ✅ Complete (20/20 tests green on the gate filter) | — | — | `alembic upgrade head` clean to 0010; `pytest -k "workflow or signature or audit or migration"` → 20 passed; manual UPDATE/DELETE/TRUNCATE on audit_log all rejected by triggers | Branch `fix/phase-2-security`. Four findings closed across four commits. |
| F-007 + F-008 | High+High | ✅ Fixed (cluster) | `backend/app/models/workflow.py` (values_callable on 3 SQLEnum cols + record_hash column + compute_hash now folds record_hash into payload); `backend/app/services/signature_service.py` (entity loaders, sign-time record hash via `services.security.record_hash`, verify-time recompute + structured failure reasons); `backend/alembic/versions/0008_workflow_record_hash.py`; `backend/tests/test_signature_record_binding.py` (10 tests) | `3e5256c` | 10/10 pass | F-007 needed only ORM-side change because pre-migration pg_enum was already lowercase (0001 used `postgresql.ENUM("active", …)` literals — verified). F-008 verify_signature now distinguishes `signature_not_found / hash_mismatch / record_mismatch / entity_missing / no_hasher`. |
| F-013 | High | ✅ Fixed | `backend/alembic/versions/0009_extend_interface_enums.py` (NEW); `add_interface_enum_values.ps1` deleted | `88350d7` | `alembic upgrade head` 0008 → 0009 success; `enum_range` count parity 91/76/15 (connectortype/signaltype/pindirection) — identical to dev DB pre-migration | 50 ALTER TYPE … ADD VALUE statements moved into `op.get_context().autocommit_block()`. IF NOT EXISTS makes re-runs idempotent. Downgrade is a no-op (Postgres can't cleanly remove enum values; documented). |
| F-022 | High | ✅ Fixed | `backend/alembic/versions/0001_initial_schema.py:downgrade()` + `0007_interface_module.py:downgrade()` (env-var guard); `backend/tests/test_migration_guards.py` (6 tests parametrized over both migrations) | `46fba8b` | 6/6 pass | Guard fires BEFORE any `op.*` call so test invokes `downgrade()` directly via importlib without an Alembic context. Override path: `ASTRA_ALLOW_DESTRUCTIVE_DOWNGRADE=true`. |
| F-009 | High | ✅ Fixed | `backend/alembic/versions/0010_audit_append_only_triggers.py` (NEW — installs the three plpgsql functions + triggers from `database/migrations/audit_append_only.sql`) | `b754ae0` | `\d+ audit_log` shows all three triggers attached + enabled; manual UPDATE / DELETE / TRUNCATE all rejected with the AU-9 message; INSERTs (record_event) keep working (251 rows survive); pre-flight grep confirmed no app code path UPDATE/DELETE/TRUNCATEs audit_log | Sqlite-backed pytest doesn't exercise plpgsql triggers — the manual psql verification is the canonical proof. |
| F-058 | Medium | ✅ Fixed | (folded into F-014 part 1 commit `449d6ad` — integrations.py changes) | `449d6ad` | n/a | create/update integration endpoints now call `_check_membership` on the body's project_id. |
| F-032 | Medium | ✅ Fixed (durability closed in 2C) | (cross-project leakage closed in `449d6ad` reports.py change). Durability closed alongside F-019 in 2C. | `449d6ad`, F-019 commit | n/a | `_report_history` module list removed; `/reports/history` now reads from `report_jobs` DB table (durable, cross-worker, project-membership-scoped). See F-019 row for the full file list. |
| F-062 | Medium | ✅ Fixed | (folded into F-004 commit `ad5a5b9`) | `ad5a5b9` | n/a | Idempotency-by-count replaced with SEED-MARKER sentinel. |
| F-120 | Low | ✅ Fixed | (folded into F-004 commit `ad5a5b9`) | `ad5a5b9` | n/a | Router prefix moved from `/dev` to `/admin/seed-project`. |
| F-121 | Low | ✅ Fixed | (folded into F-002 commit `d495747`) | `d495747` | n/a | Replaced silent `except: pass` in main.py optional-router/model/middleware loops with `logger.warning`. |
| F-138 | Info | ✅ Fixed | (folded into F-002 commit `d495747`) | `d495747` | n/a | `from app.models import ApprovalWorkflow` etc. now resolves. |
| **Phase 2C — Reliability & availability** | — | ⏳ In progress | — | — | — | Branch `fix/phase-2-security`. F-018 closed; F-019+F-032 and F-020 next. |
| F-018 | High | ✅ Fixed | `backend/app/services/security/spreadsheet.py` (NEW — sniff_content_type, validate_upload, assert_workbook_size_ok, formula_safe, sanitize_filename); `backend/app/main.py` (register `BodySizeLimitMiddleware` after CORS, before any router); `backend/app/routers/imports.py` (preview_import wires validate_upload + sanitize_filename + assert_workbook_size_ok in `_parse_xlsx`); `backend/app/routers/interface_import.py` (preview + confirm both validate, sanitize, and size-cap; `request: Request` added as positional parameter to `confirm_import` so FastAPI recognises it); `.env.example` (MAX_UPLOAD_BYTES, MAX_UPLOAD_ROWS, MAX_UPLOAD_SHEETS); `backend/tests/test_upload_hardening.py` (NEW — 27 tests across sniff/formula_safe/sanitize_filename units, body-size middleware, content-type validation, workbook size caps) | `e119958` | `pytest backend/tests/test_upload_hardening.py` → 27/27 pass; `pytest -k "import or upload"` → 32/32 pass (no regressions in interface excel import/export) | Layered defenses: (1) BodySizeLimitMiddleware short-circuits oversize Content-Length with 413 before the route reads, (2) validate_upload cross-checks declared MIME against magic-byte sniff (XLSX = `PK\x03\x04`, CSV = printable ASCII) — declared XLSX with CSV bytes now 415s correctly (initial implementation only checked declared and sniffed independently — fixed before commit), (3) assert_workbook_size_ok caps sheets/rows post-parse, (4) formula_safe prefixes `=+-@` per OWASP CSV-injection guidance, (5) sanitize_filename basename + `[A-Za-z0-9._-]` allowlist for echoed names. No `python-magic` dependency added — bytes-based sniff is sufficient for the two formats. **New finding logged:** `confirm_import` originally had `request: Request \| None = None` — FastAPI rejected it as an invalid response field. Fixed by making it positional `request: Request`. |
| F-020 | High | ✅ Fixed | `backend/app/routers/audit.py` (`/audit/export` rewritten — `_iter_audit_rows` yields from `db.query(...).order_by(seq).yield_per(500)` server-side cursor; `_stream_csv` and `_stream_ndjson` are byte-yielding generators wrapped in `StreamingResponse`; `format=json` now returns `application/x-ndjson` one-record-per-line; per-project membership enforced via `_check_membership` when project_id is supplied; admin global export with no project_id still works); `frontend/src/app/projects/[id]/audit/page.tsx` (handleExportJSON now requests `responseType: blob` and writes `.ndjson` so it doesn't materialise the audit log in browser memory or call JSON.stringify on it); `backend/tests/test_audit_export.py` (NEW — 7 tests: CSV header + rows, CSV empty, CSV project-filter; NDJSON one-record-per-line + integrity, NDJSON empty; non-member 403; admin no-project-id sees all rows). | `d43fb8f` | `pytest backend/tests/test_audit_export.py` → 7/7 pass; `pytest -k "audit or report or upload or import"` → 51/52 pass (only failure is the pre-existing `test_auto_wire_reports_unmatched`); manual smoke `curl -X POST -H "Content-Length: 104857600"` against live backend confirms F-018 middleware returns `413 {"detail":"Request body too large …"}` end-to-end | NIST AU-2/AU-9 export-at-scale fix. NDJSON is a breaking change for any non-frontend consumer that did `JSON.parse(body)` on the prior single-array payload — frontend updated in the same commit. The token "json" is preserved on the URL so existing bookmarks keep working. INSERT-only test pattern per the 2C rule (post-0010 audit_log triggers reject UPDATE/DELETE/TRUNCATE on the real PG backend; sqlite tests follow the same rule for consistency). |
| F-019 + F-032 | High+Medium | ✅ Fixed (cluster) | `backend/app/models/report_job.py` (NEW — ReportJob model + ReportJobStatus enum); `backend/alembic/versions/0011_report_jobs.py` (NEW — table + reportjobstatus PG enum + 4 indexes); `backend/app/services/reports/jobs.py` (NEW — create_pending_job, run_job, record_completed, get_job_for_user with own-session pattern for BackgroundTask safety); `backend/app/routers/reports.py` (full rewrite — `_report_history` removed, every sync GET endpoint now writes a COMPLETED `report_jobs` row via `record_completed`, new POST `/reports/{type}/jobs` returns 202 + job_id and runs generator in BackgroundTasks, new GET `/reports/jobs/{id}` polls status, new GET `/reports/jobs/{id}/download` streams blob, `/reports/history` now reads from DB scoped by `_check_membership`); `backend/app/main.py` (register `app.models.report_job` in metadata-import list); `backend/tests/test_report_jobs.py` (NEW — 10 tests covering sync persistence, history scoping, async POST/poll/download round-trip, unknown report type 404, non-member 403 on enqueue + view + download, generator-failure → status=failed + 409 on download) | `ff95f67` | `alembic upgrade head` 0010 → 0011 success; `\d report_jobs` shows 4 indexes + reportjobstatus enum; `pytest backend/tests/test_report_jobs.py` → 10/10 pass; `pytest -k "report or import or upload or history"` → 43/44 pass (only failure is `test_interface.py::TestAutoWire::test_auto_wire_reports_unmatched`, confirmed pre-existing — same failure on stash-popped clean state — logged as new finding) | F-019 timeout addressed by async path (BackgroundTasks); F-032 durability addressed by DB-backed history. Frontend kept on the existing sync GET flow — no breaking change; sync calls now also persist a `report_jobs` row so the same row powers both the in-request audit and the durable history list. The async POST flow is opt-in for clients that want to avoid the timeout problem. ReportJob blob is stored inline as `LargeBinary`; can be swapped to S3 later without changing the public API (job_id + download endpoint stay stable). BackgroundTasks open their own SessionLocal — tests monkeypatch `app.services.reports.jobs.SessionLocal` to bind to the per-test sqlite engine. |
| **Phase 2D — Workflow integrity, schema hygiene, triage cleanup** | — | ✅ Complete | — | — | full pytest 221/222 (only F-144 left — Phase 3) | Branch `fix/phase-2-security`. 11 findings closed across 11 commits. |
| F-035 | Medium | ✅ Fixed | `backend/app/models/__init__.py` (TraceLink: project_id FK + project relationship + UniqueConstraint uq_trace_link_endpoints + 3 indexes; Index/UniqueConstraint added to imports); `backend/app/routers/projects.py` (new `_resolve_entity_project` polymorphic helper covering requirement/source_artifact/verification; `create_trace_link` now validates source/target exist + same-project + not-duplicate + membership; persists project_id on the link); `backend/alembic/versions/0012_trace_link_project_id.py` (NEW — ADD COLUMN nullable + backfill from source entity + SET NOT NULL + FK CASCADE + UNIQUE + index; aborts with RuntimeError if any row remains unresolvable); `backend/tests/test_trace_link_integrity.py` (NEW — 7 tests). | `fad9003` | 7/7 tests pass; `\d trace_links` shows project_id NOT NULL, FK CASCADE, UNIQUE constraint, ix_trace_links_project, legacy ix_tracelink_pair gone | Closes the cross-project trace-link leak + dangling-reference + duplicate-link gaps in one shot. |
| F-142 | High (new) | ✅ Fixed | `backend/app/routers/interface.py` (`batch_create_wires` pre-fetches each batch pin's `connector_id`, rejects same-connector wires, and for legacy 2-endpoint harnesses enforces strict from/to-connector matching; multi-endpoint harnesses keep relaxed cross-connector behavior). | `a260ace` | originally-failing `test_wire_wrong_connector_rejected` now passes; happy-path `test_batch_create_wires` still green | F-142 was logged as a new finding during 2C verification (real bug, not in original audit). |
| F-143 / F-078 | Medium | ✅ Fixed | `backend/app/models/interface.py` (WireHarness `__table_args__` emptied + comment pointing to `harness_endpoints.lru_connector_id` UNIQUE); `backend/alembic/versions/0013_drop_wire_harness_connector_unique.py` (NEW — DROP CONSTRAINT IF EXISTS uq_harness_connectors). | `a260ace` | originally-failing `test_auto_wire_matches_by_signal_name`, `…_skips_spares`, `…_reports_unmatched` all pass after constraint drop | The audit finding was F-078; F-143 (logged during 2C) is the same root cause. Three test failures collapsed into one fix. |
| F-145 | Low (new) | ✅ Fixed | `backend/tests/conftest.py` (`make_user` gains optional `project=` keyword that inserts a `ProjectMember(project_id, user_id, added_by_id=project.owner_id)` row); `backend/tests/test_rbac.py` (stakeholder_read_only and engineer_can_update_requirement now pass `project=test_project`; reviewer test gets the same treatment so its 403 comes from RBAC role denial not membership denial). | `88c556d` | RBAC suite now 4/4 pass | Test-side bug; production behaviour was correct all along. |
| F-036 | High | ✅ Fixed | `backend/app/models/step_up_token.py` (NEW — StepUpToken model with token_hash + expires_at + consumed_at); `backend/alembic/versions/0014_step_up_tokens.py` (NEW — table + 2 indexes); `backend/app/services/signature_service.py` (EXTERNAL_IDP_SENTINEL constant, STEP_UP_TOKEN_TTL=5min, `issue_step_up_token`, `_consume_step_up_token`, `request_signature` accepts `step_up_token` keyword and skips password verify for IdP users; reject IdP users on the password path cleanly to avoid bcrypt-on-sentinel exception); `backend/app/services/workflow_engine.py` (`perform_action` threads `step_up_token` to request_signature); `backend/app/routers/workflows.py` (new `POST /workflows/signatures/idp-step-up` returns `{step_up_token, expires_at, ttl_seconds}`; ActionPerform schema gains optional `step_up_token`); `backend/app/main.py` (register model in metadata-import list); `backend/tests/test_idp_step_up.py` (NEW — 7 tests). | `6c2882e` | 7/7 + signature_record_binding 10/10 still green | One-time-use, 5-minute TTL token. Token plaintext never persisted (only SHA-256). The IdP fresh-auth gate is forward-looking (Phase 3 IdP integration); current MVP issues to any authenticated IdP user. |
| F-037 | High | ✅ Fixed | `backend/app/services/auth_providers/piv.py` (`_load_ca_bundle` parses PEM via regex-split + per-block load; `validate_cert_chain` uses `cryptography.x509.verification.PolicyBuilder().store(Store(cas)).build_client_verifier()` for real chain walk; reject in production when bundle missing/empty/unparseable; older-cryptography fallback also refuses in prod); `backend/tests/test_piv_chain_validation.py` (NEW — 5 tests building self-signed CA + leaf with full extensions). | `01dad32` | 5/5 pass | Cryptography 43.0.3 ships with the verification module. OCSP/CRL is intentionally out of scope; tracked separately under PIV_REQUIRE_OCSP. |
| F-065 | Medium | ✅ Fixed | `backend/app/models/workflow.py` (WorkflowStage.auto_escalate_to_role removed); `backend/app/services/workflow_engine.py` (check_timeouts simplified — every timed-out stage now transitions to TIMED_OUT); `backend/app/routers/workflows.py` (StageCreate/StageUpdate schemas drop the field; default 4-stage seed and _stage_to_dict cleaned up); `backend/alembic/versions/0015_drop_workflow_auto_escalate_role.py` (NEW). | `ae5bbd7` | 17/17 workflow-touching tests still pass | Field was read but never honoured (no notification, no role flag, no DB record) — half-implemented escalation removed cleanly. Real escalation deferred to a future workflow-notifications feature. |
| F-038 | Low | ✅ Fixed | SQLAlchemy column defaults: `Project.config`, `SourceArtifact.participants`, `AIAnalysis.result_json`, `AuditLog.action_detail`, `RequirementEmbedding.embedding`, `AISuggestion.metadata_json`, `ImpactReport.report_json`, `SyncLog.details`, 4 interface metadata_json columns — all swapped from `default={}` / `default=[]` to `default=dict` / `default=list` callables. Pydantic class fields swapped to `Field(default_factory=...)` across `routers/imports.py`, `routers/integrations.py`, `routers/interface_import.py`, `schemas/__init__.py`, `schemas/ai_embeddings.py`, `schemas/ai_writer.py`, `schemas/impact.py`, `schemas/interface.py` (45+ fields total — schemas/impact + schemas/interface bulk-rewritten via temporary regex script that ran inside the container). `middleware/audit_middleware.py` ContextVar default `{}` → `None` with getter returning fresh `{}` on miss. | `8700113` | 29/29 regression on requirements/traceability/dashboard/baselines tests | Pydantic V2 actually deep-copies these so they were safe in practice; F-038 is a clarity/forward-compat sweep. Local function variables left as-is (per-call locals, not the shared-mutable-default footgun). |
| F-074 | High | ✅ Fixed | `backend/app/models/id_sequence.py` (NEW — IdSequence model with composite PK on (project_id, prefix)); `backend/alembic/versions/0016_id_sequences.py` (NEW); `backend/app/services/id_sequence.py` (NEW — `next_human_id` with `with_for_update()` + lazy backfill from existing source-table data); `backend/app/routers/interface.py` and `interface_import.py` (`_next_id` definitions delegate to `next_human_id`, preserving signatures so all 11 call sites compile unchanged); `backend/app/routers/projects.py` (create_artifact replaces `count + 1` with `next_human_id(prefix=f"ART-{project.code}")`); `backend/app/main.py` + `backend/tests/conftest.py` (register the new model so test fixtures create the table); `backend/tests/test_id_sequence.py` (NEW — 6 tests). | `88c556d`-ancestor + `…` | 6/6 pass; integration test confirms artifact-id sequence keeps climbing across deletes (no ID reuse) | Real cross-process serialisation requires Postgres FOR UPDATE row lock; sqlite tests document the single-connection limitation. Threaded burst test attempted but doesn't model real concurrency on sqlite (StaticPool deadlocks on duplicate INSERT) — replaced with serial 50-call distinct-IDs test. |
| F-075 | Medium | ✅ Fixed | `backend/app/models/__init__.py` (Requirement.__table_args__ now declares `uq_req_per_project UNIQUE (project_id, req_id)` + 3 composite indexes including the missing `(project_id, owner_id)`); `backend/alembic/versions/0017_requirements_uniqueness.py` (NEW — drops legacy non-unique ix_req_project_reqid, creates UNIQUE constraint, adds owner index); `backend/tests/test_requirement_uniqueness.py` (NEW — 3 tests). | `7e04ae2` | 3/3 + 25/25 regression on requirements/traceability/baselines tests | Operators with pre-existing duplicate (project_id, req_id) rows must dedupe before upgrading — the audit's intent is that no such dupes SHOULD exist. |
| F-076 | Medium | ✅ Fixed | `backend/app/models/__init__.py` (Project.owner_id nullable + ondelete=SET NULL; SourceArtifact.project_id ondelete=CASCADE + created_by_id SET NULL; Verification.requirement_id CASCADE + responsible_id SET NULL; RequirementHistory.requirement_id CASCADE); `backend/app/models/audit_log.py` (project_id SET NULL, user_id nullable + SET NULL — AU-9 immutability is about row-level trigger, not FK persistence); `backend/alembic/versions/0018_fk_ondelete_strategies.py` (NEW — drop+recreate each FK with new ondelete + nullable changes); `backend/tests/test_fk_ondelete.py` (NEW — 6 tests). | `fb3fe13` | 6/6 pass; pg_constraint check confirms confdeltype values | Interface-module FKs (15 tables) NOT swept — defer cascading-project-delete-through-interface to Phase 3. |
| Schema-drift cleanup | — | ✅ Fixed | `backend/alembic/env.py` (added 3 new models to optional_models list — without this `alembic check` reported them as "removed table"); `backend/alembic/versions/0019_schema_drift_cleanup.py` (NEW — CREATE INDEX IF NOT EXISTS for ix_audit_entity / _project / _user / _seq, ix_esig_entity, ix_wf_instance_entity); model `__table_args__` reconciled across `audit_log.py` (4 missing indexes), `__init__.py` (RequirementHistory + Baseline + Comment + TraceLink), `interface.py` (Wire + HarnessEndpoint + Connection — 6 indexes + 2 UNIQUEs), `workflow.py` (WorkflowInstance — 2 indexes), `auth_models.py` + `security_models.py` + `embedding.py` (3 cosmetic name-only UNIQUE drifts replaced with explicitly-named UniqueConstraints). | `cbaba8a` | After: 0 "removed" items in alembic check; residual drift is ~22 PK-redundant `ix_<table>_id` declarations + server-default notation deltas (cosmetic; not chasing) | Per the operating rule — surfaced the residual cosmetic drift rather than mass-editing every model. |

## New findings discovered during remediation

| Date | Finding | Severity | Status |
|---|---|---|---|
| 2026-04-29 | After `docker compose down` + `up -d`, the postgres `astra` user's password reverts from the value set via `ALTER USER` to whatever was set on the prior boot — re-issuing the ALTER on the new container fixed it. Suspect: `database/init.sql` mounted at `/docker-entrypoint-initdb.d/01-init.sql` may be re-running on volume reattach, OR the postgres image is honouring `POSTGRES_PASSWORD` env-var on subsequent boots when it shouldn't. This means any password-rotation workflow that bounces the db container will silently lose the new password. | Medium | **Diagnosis (2026-04-29, this session):** Both initial suspicions ruled out. (a) `database/init.sql` contains ONLY `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"` and `…"pg_trgm"` — zero `ALTER USER` / `ALTER ROLE` / `CREATE ROLE` / password references. (b) `docker-entrypoint.sh` checks `if [ -s "$PGDATA/PG_VERSION" ]` and sets `DATABASE_ALREADY_EXISTS='true'` (line 246-247); the entire init flow including any password set is gated by `if [ -z "$DATABASE_ALREADY_EXISTS" ]` (line 342) so subsequent boots SHOULD skip it. (c) Volume `astra_postgres_data` correctly persists as a named docker volume (bind: `/var/lib/docker/volumes/astra_postgres_data/_data → /var/lib/postgresql/data`); rows in non-truncated tables and the truncated-state of the 4 wiped tables both survived the bounce, confirming the volume is intact. (d) `pg_hba.conf` shows `local all all trust` and `host all all 127.0.0.1/32 trust`, with `host all all all scram-sha-256` for everything else — this is what made the earlier "both passwords work" psql probe misleading: localhost auth bypassed the password check entirely. The actual TCP path from `astra-backend-1 → db` (docker bridge) does require scram-sha-256 password match. (e) Current astra password is SCRAM-SHA-256 (133-char hash in `pg_authid`), `password_encryption = scram-sha-256`. So the revert is NOT explained by init.sql, entrypoint, or volume loss. **Most likely Phase 2 root-cause hypothesis:** WAL flush race during `docker compose down` — although postgres is supposed to fsync WAL on COMMIT, an aggressive container stop combined with `fsync = off` somewhere could lose the password change while persisting the TRUNCATE. **Fix candidates for Phase 2:** (1) check / explicitly set `fsync = on`; (2) document a "always re-run ALTER USER after compose up" runbook; (3) move POSTGRES_PASSWORD rotation into an init-time bootstrap script that idempotently re-asserts the password from env on every boot. **Workaround already used in this session's rotation:** re-issued ALTER USER on the recreated container; password held permanently after that. **Phase 2 hardening addition (logged here for backlog):** the `pg_hba.conf` line `host all all 127.0.0.1/32 trust` is itself a security smell — combined with the `local all all trust` rule, it lets anyone with shell access inside the db container (or anyone tunneling to the host's exposed 5432 port — see F-104) connect as `astra` with no password. Replace with `host all all 127.0.0.1/32 scram-sha-256`. This trust rule also explains why the first `ALTER USER` *appeared* to succeed in initial verification: the `psql -h localhost -U astra` probes were hitting the trust rule, not validating the password — so I had no actual evidence the new password took effect until the backend (which connects via the docker-bridge `db` hostname and hits the `scram-sha-256` rule) tried to authenticate post-bounce. Future rotation runbooks should verify ALTER USER via a connection that exercises the `host all all all scram-sha-256` rule (e.g. `docker exec astra-backend-1 sh -c 'PGPASSWORD=$NEW psql -h db -U astra -c "select 1"'`). |
| 2026-04-29 | `astra-pgadmin-1` container is in a permanent restart loop. **Diagnosis:** `PGADMIN_DEFAULT_EMAIL=admin@astra.local` is rejected by pgadmin's email validator because `.local` is an IANA-reserved special-use TLD (RFC 6762). Container exits 1 on every attempt with the loop message: *"'admin@astra.local' does not appear to be a valid email address. Please reset the PGADMIN_DEFAULT_EMAIL environment variable and try again."* Pre-dates the F-006 rotation — was already restart-looping before the session started. **Fix in Phase 2:** change `PGADMIN_DEFAULT_EMAIL` in `.env` and `.env.example` to a non-reserved TLD (e.g. `admin@astra.dev` or `admin@example.com`). One-line .env change + `docker compose up -d --force-recreate pgadmin`. | Low | Diagnosis only this session — fix deferred to Phase 2 per task instructions. |
| 2026-04-30 | **F-142 — Wire-create endpoint accepts pins that don't belong to the wire's declared from/to connectors.** `tests/test_interface.py::TestWireHarness::test_wire_wrong_connector_rejected` posts a wire with mismatched pins and expects 400; gets 201. Endpoint never validates `pin.connector_id ∈ {wire.from_connector_id, wire.to_connector_id}`. Real-bug, not in original 121-finding audit. | High | Diagnosis only this session. Real correctness bug — silently allows wires with internally inconsistent pin/connector references. Fix in **Phase 2D**: in `routers/interface.py` wire-create handler, after loading the from/to Pin rows, assert each pin's `connector_id` matches the corresponding wire endpoint connector; return 400 on mismatch. Add a parametric test for both wrong-from and wrong-to. |
| 2026-04-30 | **F-143 — `auto_grow.py:711` creates duplicate WireHarness rows on the same `(from_connector_id, to_connector_id)` pair, raising `IntegrityError` and 500.** Cascades through `tests/test_interface.py::TestAutoWire::test_auto_wire_matches_by_signal_name` (500 directly), `…::test_auto_wire_skips_spares` (KeyError on missing `wires_created` in 500 response), `…::test_auto_wire_reports_unmatched` (engine bails before computing `unmatched_from`). Real bug, not in original audit. | Medium | Diagnosis only this session. Three test failures, one root cause. Fix in **Phase 2D**: in `services/interface/auto_grow.py:_create_harness_with_endpoints`, pre-check for an existing WireHarness on `(from_connector_id, to_connector_id)` and reuse it; OR catch `IntegrityError`, roll back the SAVEPOINT, and SELECT the existing row. The router-side `try/except` at `interface.py:2791` should also map `IntegrityError` to a 409-with-detail rather than 500. |
| 2026-04-30 | **F-144 — Unit-spec env-req generator produces 2 of 3 expected requirements.** `tests/test_interface.py::TestAutoRequirementGeneration::test_unit_specs_create_env_requirements` calls `AutoRequirementGenerator.on_unit_created(test_unit)`, expects ≥3 env/EMI reqs (the test then asserts a "Temperature" title is among them, implying temp + vibration + ≥1 EMI), gets 2. Either the `test_unit` fixture lost a spec field or the generator dropped a req type. | Low | Diagnosis only — needs per-side investigation. Read `test_unit` fixture (likely in `tests/test_interface.py` or `conftest.py`) for `temp_min/max`, `vibration_g`, EMI fields → cross-check `AutoRequirementGenerator.on_unit_created` for the corresponding branches. Resolution will be either (c) test-infra (fixture stale) or (a) generator-drift (branch removed). Defer to **Phase 3** alongside the broader interface-quality sweep. |
| 2026-04-30 | **F-145 — `tests/conftest.make_user` helper creates a User but doesn't add a ProjectMember row, so post-F-014 RBAC tests against project-scoped endpoints get 403 "Not a project member" instead of the role-based 403/200 they expect.** Surfaces as `tests/test_rbac.py::TestRBAC::test_stakeholder_read_only` (403 vs 200 on stakeholder GET) and `…::test_engineer_can_update_requirement` (403 "Not a project member" vs 200 on engineer PATCH). Note `test_admin_can_create_requirement` and `test_reviewer_cannot_create_requirement` *appear* to pass but for the wrong reasons (admin bypasses membership; reviewer's expected 403 happens to match a membership 403, not the intended RBAC 403). Test-side bug — production behaviour is correct. | Low | Diagnosis only this session. Fix in **Phase 2D** alongside other Phase-2 sweep items: extend `make_user` to optionally accept a `project: Project` argument and insert the corresponding `ProjectMember(project_id=…, user_id=…, added_by_id=…)` row, OR add a `project_member` fixture per-class. Also re-verify `test_reviewer_cannot_create_requirement` once the membership exists (the 403 should still fire from RBAC, not membership — this proves the role enforcement is real). |

## Phase status

- [x] Phase 1 — Critical Foundation (merged to main as `c9b59c0`; F-006 fully resolved including history rewrite; F-014 verification test 47/47 green)
- [~] Phase 2 — Security Hardening + Compliance (branch `fix/phase-2-security`)
  - [x] 2A — Auth & access surface (F-015, F-016+F-031+F-124, F-017, F-021+F-104, F-066) — 22/22 tests green
  - [x] 2B — Compliance integrity (F-007+F-008 cluster, F-013, F-022, F-009) — 20/20 tests green + manual psql triggers verified
  - [x] 2C — Reliability & availability (F-018, F-019+F-032 cluster, F-020) — 44/44 tests green; live `curl -X POST -H "Content-Length: 104857600"` confirms F-018 middleware returns 413 end-to-end. 7 pre-existing test_interface / test_rbac failures surfaced during the verification gate; triaged into F-142 (High, 2D), F-143 (Medium, 2D — covers 3 of the 7), F-144 (Low, Phase 3), F-145 (Low, 2D — covers the remaining 2).
  - [x] 2D — Workflow integrity, schema hygiene, triage cleanup (F-035, F-142, F-143/F-078, F-145, F-036, F-037, F-065, F-038, F-074, F-075, F-076, schema-drift) — full pytest 221/222 (only F-144 remains, routed to Phase 3); alembic head 0019; 6 of 7 originally-failing tests now passing; alembic check residual drift is cosmetic only (PK-redundant `ix_<table>_id` declarations + server-default notation deltas).
  - [ ] 2D — Hardening + sweep (F-010, F-011, F-012, F-024, F-025, F-026, F-027, F-028, F-029, F-030, F-033 onwards as scoped)
- [ ] Phase 3 — Medium Severity
- [ ] Phase 4 — Low Severity & Cleanup
- [ ] Phase 5 — Info & Follow-ups
- [ ] Post-remediation audit re-run

## Deferred items requiring user coordination

| Finding | Reason | Action required from Mason |
|---|---|---|
| F-014 verification test (Docker run) | Local Python 3.14 cannot install pinned pydantic-core==2.27 / psycopg2-binary wheels — must run inside `astra-backend-1` | `docker exec astra-backend-1 pytest backend/tests/test_project_membership.py -v`. All 41 parametrized cases plus 4 supplementary tests must pass. If any fail, do not merge `fix/phase-1-critical` to `main`. |

## Secret rotation log (F-006 stage 2 — 2026-04-29)

Treat the leaked dump as a full credential compromise. Rotated:

| Secret | Old | New | Where it now lives |
|---|---|---|---|
| `SECRET_KEY` (JWT signing) | `replace-with-64-char-random-string-openssl-rand-hex-32` (placeholder, was in `_WEAK_SECRETS`) | new 64-hex-char value | `.env` only (gitignored) |
| `ENCRYPTION_KEY` (Fernet field encryption) | `<openssl rand -hex 32>` literal placeholder | new 64-hex-char value | `.env` only |
| `POSTGRES_PASSWORD` | `astra_dev_password_change_me` | new 48-hex-char value | `.env` + DB ALTER USER on `astra-db-1` |
| `PGADMIN_DEFAULT_PASSWORD` | `pgadmin_change_me` | new 48-hex-char value | `.env` only (pgAdmin container is in a restart loop unrelated to the rotation — see below) |

`.env` confirmed gitignored via `git check-ignore -v .env` and `git status` shows it untracked. **Never commit `.env`.**

Hex (not base64) for all four — avoids URL-escaping inside `DATABASE_URL`.

### Tables truncated (data-encrypted-under-old-key)

```sql
BEGIN;
ALTER USER astra WITH PASSWORD '<new>';
TRUNCATE integration_configs, mfa_configs, electronic_signatures,
         refresh_tokens RESTART IDENTITY CASCADE;
COMMIT;
```

Post-truncate row counts (verified 0 each):
`integration_configs=0`, `mfa_configs=0`, `electronic_signatures=0`, `refresh_tokens=0`.

Other tables (`users`, `audit_log`, `auth_sessions`, `account_lockouts`, `requirements`, etc.) intact — bcrypt password hashes are not Fernet-encrypted under `ENCRYPTION_KEY` so they survive the key rotation. `mason / password123` login confirmed working post-rotation (`POST /api/v1/auth/login` returns 200 + JWT).

### User action items (NOT performed by Claude Code)

These require coordination outside this remediation:

1. **Reissue every Jira / Azure DevOps / DOORS API token** that was previously stored in `integration_configs.config_encrypted`. The encrypted column is gone but the upstream token is now considered exposed (the dump had the encrypted blob *and* leaked the encryption key). Rotate at the upstream provider and re-add via `POST /api/v1/integrations/`.
2. **Every user with active MFA must re-enroll.** `mfa_configs` was truncated. Affected users will see MFA disabled on next login; have them re-run the enrollment flow.
3. **Every active session is invalidated.** `refresh_tokens` was truncated, and all in-flight JWTs were signed with the old `SECRET_KEY` so verification will fail. Users must log in again.
4. **Every electronic signature is gone.** `electronic_signatures` was truncated. Any in-flight 21 CFR Part 11 approval workflows that referenced a signature row will now fail integrity verification — those approvals must be re-collected. (Per F-008, the signatures didn't bind record state anyway — Phase 2 §4.12 fixes that.)
5. **Decide on the local copy at `C:/Users/Mason/Documents/ASTRA-backups/4_24_2026_SQL_ASTRA.sql`.** It is no longer in git but the binary still exists on disk. If it's no longer needed, `rm` it; otherwise move it to encrypted storage.
6. **Notify any collaborators with local clones** that they must `git fetch && git reset --hard origin/<branch>` (or re-clone) — every commit SHA on `main` and `fix/phase-1-critical` changed. Old SHAs are no longer reachable on origin.

### Anomalies observed during rotation

- **pgAdmin container is in a restart loop** (`Restarting (1)` for several minutes after the rotation). This is independent of the rotation — pgAdmin was already restart-looping before the rotation started (visible in the very first `docker ps`). Not blocking. Triage separately.
- **Postgres password reverted on container recreation.** After `docker compose down` + `up -d`, the first `ALTER USER` did not survive the bounce — the password was back to the old value when backend tried to TCP-connect. Re-issuing `ALTER USER astra WITH PASSWORD '<new>'` once the new container was up restored the new password permanently. Suspect either an `init.sql` re-application or a postgres image quirk on volume reattach. Worth filing as a new finding (logged below).
